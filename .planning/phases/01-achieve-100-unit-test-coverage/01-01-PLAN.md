---
phase: 01-achieve-100-unit-test-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/conftest.py
  - pyproject.toml
  - .github/workflows/ci.yml
  - skills/orchestrator/engine/engine_setup.py
  - skills/orchestrator/engine/platforms/inject_utils.py
  - skills/orchestrator/engine/orchestrator.py
  - skills/orchestrator/engine/cli.py
  - skills/comparator/matrix_ops.py
  - skills/landscape-researcher/launch_report.py
autonomous: true
requirements:
  - COV-INFRA
  - COV-PRAGMA
  - COV-CI

must_haves:
  truths:
    - "tests/conftest.py provides mock_page fixture usable by all platform driver tests"
    - "pyproject.toml has [tool.pytest.ini_options] with asyncio_mode = auto"
    - "pyproject.toml has [tool.coverage.run] covering all 3 source trees"
    - "All pragma: no cover lines are genuinely untestable"
    - "_stub_engine_setup is a plain callable function, NOT an auto-applied fixture"
  artifacts:
    - path: "tests/conftest.py"
      provides: "mock_page fixture, install_stubs helper, _stub_engine_setup helper"
      contains: "mock_page"
    - path: "pyproject.toml"
      provides: "pytest and coverage configuration"
      contains: "asyncio_mode"
  key_links:
    - from: "tests/conftest.py"
      to: "all platform test files"
      via: "pytest fixture injection"
      pattern: "mock_page"
---

<objective>
Create the shared test infrastructure needed by all subsequent test plans: the canonical MockPage fixture in conftest.py, pytest/coverage configuration in pyproject.toml, and pragma: no cover annotations on genuinely untestable lines.

Purpose: Every subsequent plan depends on mock_page fixture and proper pytest config. This must land first.
Output: tests/conftest.py with MockPage, pyproject.toml with coverage config, pragma annotations on all untestable lines.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-achieve-100-unit-test-coverage/01-RESEARCH.md
@skills/orchestrator/engine/platforms/base.py
@skills/orchestrator/engine/platforms/browser_utils.py
@skills/orchestrator/engine/platforms/inject_utils.py
@tests/test_orchestrator_args.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create conftest.py with MockPage fixture and shared helpers</name>
  <files>tests/conftest.py</files>
  <read_first>
    - tests/test_orchestrator_args.py (existing _import_cli pattern for sys.modules stubbing)
    - skills/orchestrator/engine/platforms/base.py (page methods used)
    - skills/orchestrator/engine/platforms/browser_utils.py (page methods used)
    - skills/orchestrator/engine/platforms/inject_utils.py (page methods used)
    - .planning/phases/01-achieve-100-unit-test-coverage/01-RESEARCH.md (Q1: MockPage design)
  </read_first>
  <action>
Create tests/conftest.py with:

1. A `mock_page` pytest fixture returning a MagicMock configured as follows (from RESEARCH Q1):
   - `page.url = "https://example.com"` (string property, not mock)
   - Async methods: `page.title = AsyncMock(return_value="Page Title")`, `page.goto = AsyncMock()`, `page.wait_for_timeout = AsyncMock()`, `page.wait_for_selector = AsyncMock()`, `page.evaluate = AsyncMock(return_value="")`, `page.bring_to_front = AsyncMock()`
   - `page.keyboard = MagicMock()` with `page.keyboard.press = AsyncMock()`, `page.keyboard.type = AsyncMock()`
   - A `_make_locator(count=0, visible=False, text="")` inner helper that returns a MagicMock with: `.first = loc`, `.count = AsyncMock(return_value=count)`, `.is_visible = AsyncMock(return_value=visible)`, `.click = AsyncMock()`, `.get_attribute = AsyncMock(return_value=None)`, `.inner_text = AsyncMock(return_value=text)`, `.evaluate = AsyncMock(return_value=None)`, `.fill = AsyncMock()`, `.dispatch_event = AsyncMock()`, `.type = AsyncMock()`, `.nth = lambda n: loc`
   - `page.locator = MagicMock(side_effect=lambda sel: _make_locator())`
   - `page.get_by_text = MagicMock(side_effect=lambda text, **kw: _make_locator())`
   - `page.get_by_role = MagicMock(side_effect=lambda role, **kw: _make_locator())`
   - `page.on = MagicMock()`
   - Frame mock: `frame = MagicMock()`, `frame.url = "about:blank"`, `frame.evaluate = AsyncMock(return_value=0)`, `page.frames = [frame]`, `page.main_frame = frame`
   - Download context manager: `dl = MagicMock()`, `dl.__aenter__ = AsyncMock(return_value=dl)`, `dl.__aexit__ = AsyncMock(return_value=False)`, `dl.value = AsyncMock(return_value=MagicMock(path=AsyncMock(return_value=None)))`, `page.expect_download = MagicMock(return_value=dl)`

2. A `_stub_engine_setup()` **callable helper function** (NOT a fixture -- must be explicitly called by each test file that needs it):
   ```python
   def _stub_engine_setup():
       """Install a fake engine_setup module into sys.modules.
       
       Returns the stub module. Callers that need the REAL engine_setup
       (e.g. Plan 03 test_engine_setup.py) MUST:
         1. sys.modules.pop("engine_setup", None)
         2. import the real module
         3. In teardown: call _stub_engine_setup() to restore the stub
       """
       import types
       es = types.ModuleType("engine_setup")
       es._load_dotenv = lambda: None
       es._ensure_venv = lambda: None
       es._ensure_dependencies = lambda: None
       sys.modules["engine_setup"] = es
       return es
   ```
   
   IMPORTANT: This must NOT be decorated with `@pytest.fixture` or use `autouse=True`.
   It is a plain function. Each test module calls it explicitly when needed.
   Plan 03's test_engine_setup.py must pop sys.modules["engine_setup"] before importing
   the real module, and restore the stub in teardown (see Plan 03 Task 3 for the pattern).

3. An `install_stubs(platform_name, url)` function that stubs `sys.modules` for:
   - `engine_setup` (via `_stub_engine_setup()`)
   - `playwright`, `playwright.async_api` (with `Page = MagicMock`, `async_playwright = MagicMock`, `BrowserContext = MagicMock`)
   - `agent_fallback` (with `AgentFallbackManager = MagicMock`, `FallbackStep = MagicMock`)
   - `browser_use` (with `Agent = MagicMock`, `BrowserSession = MagicMock`)
   - `platforms` (with `ALL_PLATFORMS` dict containing MagicMock for all 7 platforms)

4. An `ENGINE_DIR` constant: `str(Path(__file__).parent.parent / "skills" / "orchestrator" / "engine")`

5. Ensure `ENGINE_DIR` is in `sys.path` at module level.

6. Add `import pytest` and `from unittest.mock import AsyncMock, MagicMock` at top.
  </action>
  <verify>
    <automated>python3 -m pytest tests/ -v --tb=short -k "test_ut" --co 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/conftest.py exists and contains `def mock_page` fixture
    - tests/conftest.py contains `def _stub_engine_setup` as a plain function (NOT a fixture)
    - tests/conftest.py contains `def install_stubs` function
    - `grep "autouse" tests/conftest.py` returns NO matches (no auto-applied stub fixtures)
    - All 99 existing tests still pass: `python3 -m pytest tests/ -v --tb=short` exits 0
  </acceptance_criteria>
  <done>conftest.py provides mock_page, _stub_engine_setup (callable, not fixture), install_stubs; all 99 existing tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Add pytest/coverage config and pragma: no cover annotations</name>
  <files>pyproject.toml, .github/workflows/ci.yml, skills/orchestrator/engine/engine_setup.py, skills/orchestrator/engine/platforms/inject_utils.py, skills/orchestrator/engine/orchestrator.py, skills/orchestrator/engine/cli.py, skills/comparator/matrix_ops.py, skills/landscape-researcher/launch_report.py</files>
  <read_first>
    - pyproject.toml (current state)
    - .github/workflows/ci.yml (current coverage command)
    - .planning/phases/01-achieve-100-unit-test-coverage/01-RESEARCH.md (Q8 and Q9)
    - skills/orchestrator/engine/engine_setup.py (lines needing pragma)
    - skills/orchestrator/engine/platforms/inject_utils.py (line 71: unsupported platform)
    - skills/orchestrator/engine/orchestrator.py (main guard)
    - skills/orchestrator/engine/cli.py (main guard)
    - skills/comparator/matrix_ops.py (main guard)
    - skills/landscape-researcher/launch_report.py (main guard)
    - skills/orchestrator/engine/prompt_echo.py (line 73: `return False` when prompt_sigs empty)
  </read_first>
  <action>
1. **pyproject.toml** -- Add these sections after the existing `[tool.setuptools.package-data]` section:

```toml
[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.coverage.run]
source = [
    "skills/orchestrator/engine",
    "skills/comparator",
    "skills/landscape-researcher",
]
omit = [
    "*/.*",
    "*/.venv/*",
    "*/site-packages/*",
    "skills/orchestrator/engine/tests/*",
]

[tool.coverage.report]
fail_under = 100
exclude_lines = [
    "pragma: no cover",
    "if __name__ == .__main__.:",
    "raise NotImplementedError",
]
```

2. **CI yaml** -- Update the "Unit tests with coverage" step to add `--cov=skills/comparator --cov=skills/landscape-researcher` and `--cov-fail-under=100` (but NOTE: do NOT add `--cov-fail-under=100` yet -- it will be added in the final plan after all tests are written). For now, just add the extra --cov paths:

```yaml
- name: Unit tests with coverage
  run: |
    python -m pytest tests/ -v --tb=short \
      --cov=skills/orchestrator/engine \
      --cov=skills/comparator \
      --cov=skills/landscape-researcher \
      --cov-report=term-missing
```

3. **pragma: no cover annotations** -- Add `# pragma: no cover` to these specific lines:

   - `engine_setup.py` line 57 (`subprocess.check_call([sys.executable, "-m", "venv"...`) -- the `except` block on the check_call for venv creation has `sys.exit(1)` -- mark the `sys.exit(1)` line with `# pragma: no cover`
   - `engine_setup.py` lines 171-173: the `sys.exit(1)` inside `except subprocess.CalledProcessError` after "Failed to install playwright" -- add `# pragma: no cover` to the `sys.exit(1)` line
   - `engine_setup.py` lines 180-182: the `sys.exit(1)` inside `except subprocess.CalledProcessError` after "Failed to install Chromium" -- add `# pragma: no cover` to the `sys.exit(1)` line
   - `inject_utils.py` line 71: `raise RuntimeError(f"Unsupported platform for clipboard paste: {sys.platform}")` -- add `# pragma: no cover`
   - `orchestrator.py` line 303-304: `if __name__ == "__main__": main()` -- already handled by exclude_lines config
   - `cli.py` line 159-160: `if __name__ == "__main__": main()` -- already handled
   - `matrix_ops.py` line 929-930: `if __name__ == "__main__": main()` -- already handled
   - `launch_report.py` line 200-201: `if __name__ == "__main__": main()` -- already handled

   Also add `# pragma: no cover` to:
   - `engine_setup.py`: the `except Exception: pass` block at the stamp file write (around line 136) -- add to the `except Exception:` line and the `pass` line

4. **prompt_echo.py coverage note:** prompt_echo.py has 96% coverage (line 73 missing: `return False` when `prompt_sigs` is empty). This line IS testable -- add a test `test_is_prompt_echo_empty_sigs` to the existing tests/test_prompt_echo.py:
   ```python
   def test_is_prompt_echo_empty_sigs():
       assert is_prompt_echo("any text here", []) is False
   ```
   This brings prompt_echo.py to 100%.

5. **utils.py coverage note:** utils.py already has 100% coverage per baseline (verified). No action needed.

6. **Do NOT add `--cov-fail-under=100` to CI yet** -- that is the final step in Plan 07 after all tests pass.
  </action>
  <verify>
    <automated>python3 -m pytest tests/ -v --tb=short 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "asyncio_mode" pyproject.toml` returns `asyncio_mode = "auto"`
    - `grep "fail_under" pyproject.toml` returns `fail_under = 100`
    - `grep "skills/comparator" pyproject.toml` appears in [tool.coverage.run] source list
    - `grep "pragma: no cover" skills/orchestrator/engine/engine_setup.py` returns >= 3 matches
    - `grep "pragma: no cover" skills/orchestrator/engine/platforms/inject_utils.py` returns >= 1 match
    - CI yaml contains `--cov=skills/comparator`
    - CI yaml contains `--cov=skills/landscape-researcher`
    - prompt_echo.py reaches 100% coverage: `python3 -m pytest tests/test_prompt_echo.py --cov=skills/orchestrator/engine/prompt_echo --cov-report=term-missing` shows 100%
    - All 99 existing tests still pass
  </acceptance_criteria>
  <done>pyproject.toml has coverage config; CI covers all 3 source trees; genuinely untestable lines have pragma; prompt_echo.py at 100%; utils.py confirmed at 100%</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test -> production code | Tests must not modify production behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T | conftest.py | accept | Test infrastructure only, no runtime impact |
</threat_model>

<verification>
- All 99 existing tests pass
- `python3 -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); print(d['tool']['coverage']['report']['fail_under'])"` prints 100
- conftest.py provides mock_page, _stub_engine_setup (plain function), install_stubs
- prompt_echo.py at 100% coverage, utils.py at 100% coverage
</verification>

<success_criteria>
Test infrastructure ready: MockPage fixture, pytest config, coverage config, pragma annotations. Zero test regressions. prompt_echo.py and utils.py at 100%.
</success_criteria>

<output>
After completion, create `.planning/phases/01-achieve-100-unit-test-coverage/01-01-SUMMARY.md`
</output>
