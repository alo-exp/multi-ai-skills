---
phase: 01-achieve-100-unit-test-coverage
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_rate_limiter.py
  - tests/test_engine_setup.py
  - tests/test_prompt_loader.py
  - tests/test_cli.py
  - tests/test_status_writer.py
  - tests/test_launch_report.py
  - tests/test_tab_manager.py
autonomous: true
requirements:
  - COV-RATE-LIMITER
  - COV-ENGINE-SETUP
  - COV-CLI
  - COV-PROMPT-LOADER
  - COV-STATUS-WRITER
  - COV-LAUNCH-REPORT
  - COV-TAB-MANAGER

must_haves:
  truths:
    - "rate_limiter.py reaches 100% coverage"
    - "engine_setup.py reaches 100% effective coverage"
    - "cli.py reaches 100% effective coverage"
    - "prompt_loader.py reaches 100% coverage"
    - "status_writer.py reaches 100% coverage"
    - "launch_report.py reaches 100% effective coverage"
    - "tab_manager.py reaches 100% coverage"
  artifacts:
    - path: "tests/test_rate_limiter.py"
      provides: "Extended rate limiter tests covering all branches"
      min_lines: 200
    - path: "tests/test_engine_setup.py"
      provides: "engine_setup.py coverage"
      min_lines: 100
    - path: "tests/test_cli.py"
      provides: "cli.py show_budget, main, _resolve_output_dir coverage"
      min_lines: 80
    - path: "tests/test_prompt_loader.py"
      provides: "prompt_loader.py all branches"
      min_lines: 60
    - path: "tests/test_status_writer.py"
      provides: "status_writer.py coverage"
      min_lines: 30
    - path: "tests/test_launch_report.py"
      provides: "launch_report.py coverage"
      min_lines: 60
    - path: "tests/test_tab_manager.py"
      provides: "tab_manager.py coverage"
      min_lines: 50
  key_links:
    - from: "tests/test_rate_limiter.py"
      to: "skills/orchestrator/engine/rate_limiter.py"
      via: "direct import"
      pattern: "from rate_limiter import"
---

<objective>
Cover all non-Playwright, non-platform pure Python modules to 100%: rate_limiter.py (71%->100%), engine_setup.py (0%->100%), cli.py (51%->100%), prompt_loader.py (22%->100%), status_writer.py (0%->100%), launch_report.py (0%->100%), tab_manager.py (0%->100%).

Purpose: These modules are testable with standard unittest.mock -- no MockPage needed.
Output: 7 test files covering all branches of these modules, verified at 100% with zero Missing lines.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-achieve-100-unit-test-coverage/01-RESEARCH.md
@skills/orchestrator/engine/rate_limiter.py
@skills/orchestrator/engine/engine_setup.py
@skills/orchestrator/engine/cli.py
@skills/orchestrator/engine/prompt_loader.py
@skills/orchestrator/engine/status_writer.py
@skills/landscape-researcher/launch_report.py
@skills/orchestrator/engine/tab_manager.py
@tests/test_rate_limiter.py
@tests/test_orchestrator_args.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend rate_limiter tests + new engine_setup, cli, prompt_loader tests</name>
  <files>tests/test_rate_limiter.py, tests/test_engine_setup.py, tests/test_cli.py, tests/test_prompt_loader.py</files>
  <read_first>
    - skills/orchestrator/engine/rate_limiter.py (full -- identify uncovered branches from Missing column: lines 111-113, 130-132, 161-167, 194-197, 207-213, 226-227, 264-265, 298-299, 323-324, 356-382, 396, 398, 404, 414, 425, 431, 452-461, 471-482, 501, 505-506, 511-518)
    - tests/test_rate_limiter.py (existing 14 tests)
    - skills/orchestrator/engine/engine_setup.py (full -- _strip_quotes, _load_dotenv, _ensure_venv, _ensure_dependencies, _verify_playwright, _verify_browser_use)
    - skills/orchestrator/engine/cli.py (full -- show_budget, _resolve_output_dir edge cases, main)
    - skills/orchestrator/engine/prompt_loader.py (full -- all branches)
    - tests/test_orchestrator_args.py (pattern for stubbing engine_setup)
    - .planning/phases/01-achieve-100-unit-test-coverage/01-RESEARCH.md (Q2, Q3)
  </read_first>
  <action>
**A. tests/test_rate_limiter.py** -- Add tests for uncovered branches:

1. `test_load_state_version_mismatch`: Create state file with `{"version": 99}`, call `load_state()`, assert `self._state == {}`
2. `test_load_state_corrupt_json`: Write invalid JSON to state file, call `load_state()`, assert `self._state == {}`
3. `test_save_state_exception_cleanup`: Use `patch("rate_limiter.tempfile.mkstemp")` to return a path, then `patch("rate_limiter.os.fdopen", side_effect=OSError)`, assert the temp file is cleaned up (or at minimum the exception propagates)
4. `test_preflight_daily_cap_reached`: Create a platform config with daily_cap > 0, record enough usage today to hit cap, assert preflight returns `allowed=False` with "Daily cap reached" reason. Patch `rate_limiter.datetime` so `datetime.now()` returns a known time, and `rate_limiter.time` so `time.time()` returns matching epoch.
5. `test_preflight_window_budget_exhausted`: Fill the rolling window to max_requests, assert preflight blocked with "Window budget exhausted"
6. `test_preflight_exponential_backoff`: Set `consecutive_rate_limits=3` on a platform state, assert effective cooldown is `base * 2^3` = 8x
7. `test_record_usage_invalid_mode`: Call `record_usage` with mode="INVALID", assert it's treated as "REGULAR" (check log warning)
8. `test_record_usage_save_failure`: Patch `save_state` to raise, assert no exception propagates (caught internally)
9. `test_get_budget_summary`: Call `get_budget_summary("REGULAR")`, assert returns dict with all 7 platforms, each having keys: display_name, remaining, total, next_available_in, cooldown, daily_cap, tier, notes
10. `test_prune_expired_removes_old_records`: Load state with old timestamps, call `_prune_expired`, assert old records removed
11. `test_seconds_until_midnight`: Call `_seconds_until_midnight()`, assert 0 < result <= 86400
12. `test_parse_timestamp_empty`: Assert `_parse_timestamp("")` returns 0.0
13. `test_parse_timestamp_invalid`: Assert `_parse_timestamp("not-a-date")` returns 0.0
14. `test_oldest_in_window_none_state`: Assert returns None when platform has no state
15. `test_get_config_fallback_to_free`: Create RateLimiter with tier="nonexistent", assert `_get_config` falls back to free tier

Use `patch("rate_limiter.time")` for time control (set `mock_time.time.return_value = FAKE_NOW`).
Use `patch("rate_limiter.datetime")` for daily cap tests (set `mock_dt.now.return_value = known_datetime`).

**B. tests/test_engine_setup.py** -- New file:

Add `sys.path.insert(0, ENGINE_DIR)` at top. Import functions inside each test to avoid module-level os.execv.

IMPORTANT: To test the REAL engine_setup module (not the stub from conftest.py), each test must:
1. `sys.modules.pop("engine_setup", None)` to remove any stub
2. Import the real module
3. In teardown (or finally block): call `_stub_engine_setup()` from conftest to restore the stub

Pattern:
```python
from conftest import _stub_engine_setup

@pytest.fixture(autouse=True)
def _restore_stub():
    yield
    _stub_engine_setup()  # restore stub after each test
```

Tests:
1. `test_strip_quotes_double`: `_strip_quotes('"hello"') == "hello"`
2. `test_strip_quotes_single`: `_strip_quotes("'hello'") == "hello"`
3. `test_strip_quotes_none`: `_strip_quotes("hello") == "hello"`
4. `test_strip_quotes_mismatched`: `_strip_quotes("'hello\"") == "'hello\""`
5. `test_load_dotenv_reads_env_file`: Create temp .env file with `KEY=value`, patch `_PROJECT_ROOT`, call `_load_dotenv()`, assert `os.environ["KEY"] == "value"`. Clean up after.
6. `test_load_dotenv_skips_existing`: Set `os.environ["KEY"] = "original"`, create .env with `KEY=new`, assert original preserved.
7. `test_load_dotenv_handles_comments_and_blanks`: .env with `# comment`, blank line, valid `K=V`
8. `test_load_dotenv_strips_quotes`: .env with `KEY="quoted_value"`, assert stripped
9. `test_load_dotenv_no_file`: No .env file exists, assert no error
10. `test_ensure_venv_noop_in_venv`: `patch("sys.prefix", "/same")`, `patch("sys.base_prefix", "/same")`, assert no subprocess call
11. `test_ensure_venv_creates_and_execs`: `patch("sys.prefix", "/real")`, `patch("sys.base_prefix", "/base")`, patch Path.exists to return False, patch `subprocess.check_call`, patch `os.execv`, assert `os.execv` called
12. `test_ensure_venv_reexecs_existing_venv`: Patch venv_python.exists() to True, assert `os.execv` called without `subprocess.check_call`
13. `test_ensure_dependencies_playwright_present`: `patch("importlib.util.find_spec", return_value=MagicMock())`, assert no subprocess call for playwright install
14. `test_ensure_dependencies_playwright_missing_installs`: `patch("importlib.util.find_spec", return_value=None)`, patch `subprocess.check_call`, assert called with pip install playwright
15. `test_verify_playwright_cached_stamp`: Create stamp file with matching version, assert no subprocess.run call
16. `test_verify_playwright_import_fails`: Patch subprocess.run to return non-zero, assert warning printed
17. `test_verify_browser_use_ok`: Patch subprocess.run to return OK, assert no warning
18. `test_verify_browser_use_fail`: Patch subprocess.run to return non-zero, assert warning printed

**C. tests/test_cli.py** -- New file (extend beyond test_orchestrator_args.py):

Reuse `_import_cli()` pattern from test_orchestrator_args.py. Test:

1. `test_show_budget_prints_table`: Mock `RateLimiter` and its methods, call `show_budget(args)`, capture stdout, assert "Rate Limit Budget" in output
2. `test_resolve_output_dir_outside_project_exits`: Set args.output_dir to "/tmp/outside", patch _PROJECT_ROOT, assert `sys.exit(1)` called
3. `test_resolve_output_dir_default`: No task_name, output_dir within project, assert returned as-is
4. `test_main_budget_flag_exits`: Mock parse_args to return args with budget=True, patch show_budget, assert sys.exit(0) called
5. `test_main_runs_orchestrate`: Mock parse_args, orchestrate, write_status, collate, assert orchestrate called
6. `test_main_cleans_temp_prompt`: Set args.prompt_file="/tmp/test.md", create the file, run main, assert file deleted
7. `test_main_exit_code_failure`: Mock orchestrate to return all failed results, assert sys.exit(1)

**D. tests/test_prompt_loader.py** -- New file:

1. `test_load_prompt_from_text`: args.prompt="hello", assert full_prompt="hello"
2. `test_load_prompt_from_file`: Create temp file, set args.prompt_file, assert content loaded
3. `test_load_prompt_file_not_found_exits`: Set nonexistent file, assert sys.exit(1)
4. `test_load_prompt_file_too_large_exits`: Create file > 512KB, assert sys.exit(1)
5. `test_condensed_prompt_from_text`: args.condensed_prompt="short", assert condensed="short"
6. `test_condensed_prompt_from_file`: Create temp file, set args.condensed_prompt_file
7. `test_condensed_prompt_file_not_found_exits`: Nonexistent condensed file, sys.exit(1)
8. `test_condensed_prompt_defaults_to_full`: No condensed args, assert condensed == full
9. `test_prompt_sigs_from_args`: args.prompt_sigs="A,B,C", assert ["A","B","C"]
10. `test_prompt_sigs_auto_extracted`: No prompt_sigs arg, assert auto_extract called

Stub engine_setup via sys.modules before import. Use `patch("sys.argv")` for argparse.
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_rate_limiter.py tests/test_engine_setup.py tests/test_cli.py tests/test_prompt_loader.py -v --tb=short --cov=skills/orchestrator/engine/rate_limiter --cov=skills/orchestrator/engine/engine_setup --cov=skills/orchestrator/engine/cli --cov=skills/orchestrator/engine/prompt_loader --cov-report=term-missing 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - rate_limiter.py coverage = 100% (0 lines in Missing column)
    - engine_setup.py coverage = 100% (0 lines in Missing column, excluding pragma lines)
    - cli.py coverage = 100% (0 lines in Missing column)
    - prompt_loader.py coverage = 100% (0 lines in Missing column)
    - All existing 99 tests still pass alongside new tests
    - `grep -c "def test_" tests/test_rate_limiter.py` returns >= 25
    - `grep -c "def test_" tests/test_engine_setup.py` returns >= 10
  </acceptance_criteria>
  <done>rate_limiter, engine_setup, cli, prompt_loader all at 100% coverage (zero Missing lines)</done>
</task>

<task type="auto">
  <name>Task 2: Tests for status_writer, launch_report, tab_manager</name>
  <files>tests/test_status_writer.py, tests/test_launch_report.py, tests/test_tab_manager.py</files>
  <read_first>
    - skills/orchestrator/engine/status_writer.py (31 statements -- write_status function)
    - skills/landscape-researcher/launch_report.py (72 statements -- find_workspace_root, is_port_in_use, start_server, ensure_chart_data_skeleton, build_url, main)
    - skills/orchestrator/engine/tab_manager.py (63 statements -- _load_tab_state, _save_tab_state, _find_existing_tab, _ensure_playwright_data_dir)
    - tests/test_launch_report.py (if exists -- check what's already there)
  </read_first>
  <action>
**A. tests/test_status_writer.py** -- New file:

Add `sys.path.insert(0, ENGINE_DIR)` at top.

1. `test_write_status_creates_json`: Pass sample results list and tmp_path output_dir, assert status.json created with correct structure (timestamp, mode, platforms keys)
2. `test_write_status_prints_table`: Capture stdout, assert "ORCHESTRATION COMPLETE" in output
3. `test_write_status_counts_complete`: Pass 3 results (2 complete, 1 failed), assert "2/3" in stdout
4. `test_write_status_handles_zero_chars`: Result with chars=0, assert "-" displayed (not "0")
5. `test_write_status_handles_zero_duration`: Result with duration_s=0, assert "-" displayed

Sample results dict: `{"platform": "test", "display_name": "Test", "status": "complete", "chars": 1000, "file": "/tmp/test.md", "mode_used": "REGULAR", "error": "", "duration_s": 5.0}`

**B. tests/test_launch_report.py** -- Extend or create:

Check if tests/test_launch_report.py already exists. If so, extend it. Add `sys.path.insert(0, str(Path(__file__).parent.parent / "skills" / "landscape-researcher"))`.

1. `test_find_workspace_root_success`: Create tmp dir with "reports/" subdir, assert returns that dir
2. `test_find_workspace_root_failure`: Start from /tmp with no reports/ ancestor, assert RuntimeError
3. `test_is_port_in_use_true`: Start a temp server on a port, assert True
4. `test_is_port_in_use_false`: Pick unused port, assert False
5. `test_start_server_success`: Patch subprocess.Popen, patch is_port_in_use to return True after call, assert Popen called
6. `test_start_server_failure`: Patch is_port_in_use to return False, assert RuntimeError
7. `test_ensure_chart_data_skeleton_creates`: No chart-data.json exists, call function, assert file created with expected keys
8. `test_ensure_chart_data_skeleton_noop`: chart-data.json already exists, assert not overwritten
9. `test_build_url`: Assert URL format correct with encoded path
10. `test_main_no_browser`: Patch sys.argv with --no-browser, patch find_workspace_root, patch is_port_in_use to True, assert webbrowser.open NOT called
11. `test_main_with_browser`: Same but without --no-browser, assert webbrowser.open called
12. `test_main_report_not_found_warns`: Patch report path to not exist, capture stderr, assert "WARNING" in output

**C. tests/test_tab_manager.py** -- New file:

Add `sys.path.insert(0, ENGINE_DIR)`.

1. `test_load_tab_state_no_file`: Patch _TAB_STATE_FILE to nonexistent path, assert returns {}
2. `test_load_tab_state_valid_json`: Create temp JSON file with tab state, patch _TAB_STATE_FILE, assert loaded correctly
3. `test_load_tab_state_corrupt_json`: Create temp file with invalid JSON, assert returns {}
4. `test_save_tab_state`: Patch _TAB_STATE_FILE to tmp path, call _save_tab_state({"p": "url"}), assert file written with correct JSON
5. `test_find_existing_tab_found`: Create mock context with pages, set page.url to contain platform domain, assert returns that page
6. `test_find_existing_tab_not_found`: No matching pages, assert returns None
7. `test_find_existing_tab_no_domain`: Platform not in PLATFORM_URL_DOMAINS, assert returns None
8. `test_ensure_playwright_data_dir_creates_dir`: Use tmp dirs, assert pw_dir created with 0o700 permissions
9. `test_ensure_playwright_data_dir_copies_cookies`: Create mock Chrome profile with Cookies file, assert copied to pw dir
10. `test_ensure_playwright_data_dir_removes_symlink`: Create symlink at profile_dst, assert removed and replaced with dir
11. `test_ensure_playwright_data_dir_skips_nonexistent_src`: Source file doesn't exist, assert no error
12. `test_ensure_playwright_data_dir_copies_local_state`: Create Local State file, assert copied

For _find_existing_tab: use `@pytest.mark.asyncio` and create mock context with `context.pages = [mock_page]` where `mock_page.url = "https://chatgpt.com/chat"`.
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_status_writer.py tests/test_launch_report.py tests/test_tab_manager.py -v --tb=short --cov=skills/orchestrator/engine/status_writer --cov=skills/orchestrator/engine/tab_manager --cov=skills/landscape-researcher/launch_report --cov-report=term-missing 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - status_writer.py coverage = 100% (0 lines in Missing column)
    - tab_manager.py coverage = 100% (0 lines in Missing column)
    - launch_report.py coverage = 100% (0 lines in Missing column, excluding pragma lines)
    - `grep -c "def test_" tests/test_status_writer.py` returns >= 4
    - `grep -c "def test_" tests/test_tab_manager.py` returns >= 8
    - All existing tests still pass
  </acceptance_criteria>
  <done>status_writer, launch_report, tab_manager all at 100% coverage (zero Missing lines)</done>
</task>

<task type="auto">
  <name>Task 3: Final sweep -- verify 0 Missing lines across all 7 modules</name>
  <files>tests/test_rate_limiter.py, tests/test_engine_setup.py, tests/test_cli.py, tests/test_prompt_loader.py, tests/test_status_writer.py, tests/test_launch_report.py, tests/test_tab_manager.py</files>
  <read_first>
    - Output of Task 1 and Task 2 verify commands (coverage reports)
  </read_first>
  <action>
Run the combined coverage report for all 7 modules:

```bash
python3 -m pytest tests/test_rate_limiter.py tests/test_engine_setup.py tests/test_cli.py tests/test_prompt_loader.py tests/test_status_writer.py tests/test_launch_report.py tests/test_tab_manager.py \
  --cov=skills/orchestrator/engine/rate_limiter \
  --cov=skills/orchestrator/engine/engine_setup \
  --cov=skills/orchestrator/engine/cli \
  --cov=skills/orchestrator/engine/prompt_loader \
  --cov=skills/orchestrator/engine/status_writer \
  --cov=skills/orchestrator/engine/tab_manager \
  --cov=skills/landscape-researcher/launch_report \
  --cov-report=term-missing \
  -v --tb=short
```

Examine the Missing column for each module. For ANY line that still shows as missing:

1. Determine what that line does (read the source file at that line number)
2. If testable: add a targeted test to the appropriate test file
3. If genuinely untestable (e.g., platform-specific OS branch that cannot be reached, defensive except that requires hardware failure): add `# pragma: no cover` with a comment explaining why

Re-run the coverage report after fixes. Repeat until ALL 7 modules show 0 in the Missing column.

Also verify that test_engine_setup.py properly restores the engine_setup stub after each test:
- Confirm the `_restore_stub` fixture calls `_stub_engine_setup()` in teardown
- Confirm no other test file is broken by engine_setup import ordering
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_rate_limiter.py tests/test_engine_setup.py tests/test_cli.py tests/test_prompt_loader.py tests/test_status_writer.py tests/test_launch_report.py tests/test_tab_manager.py --cov=skills/orchestrator/engine/rate_limiter --cov=skills/orchestrator/engine/engine_setup --cov=skills/orchestrator/engine/cli --cov=skills/orchestrator/engine/prompt_loader --cov=skills/orchestrator/engine/status_writer --cov=skills/orchestrator/engine/tab_manager --cov=skills/landscape-researcher/launch_report --cov-report=term-missing -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - ALL 7 modules show 0 lines in the Missing column of --cov-report=term-missing
    - No test file is broken by engine_setup stub/real module switching
    - Full test suite still passes: `python3 -m pytest tests/ -v --tb=short` exits 0
  </acceptance_criteria>
  <done>All 7 non-Playwright modules confirmed at exactly 100% coverage with zero Missing lines</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test -> filesystem | Temp files in tmp_path, auto-cleaned |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | T | test files | accept | No production impact |
</threat_model>

<verification>
- All 7 modules at 100% coverage (0 Missing lines each)
- All existing + new tests pass: `python3 -m pytest tests/ -v --tb=short`
</verification>

<success_criteria>
Seven non-Playwright modules at exactly 100% coverage with zero Missing lines: rate_limiter, engine_setup, cli, prompt_loader, status_writer, launch_report, tab_manager.
</success_criteria>

<output>
After completion, create `.planning/phases/01-achieve-100-unit-test-coverage/01-03-SUMMARY.md`
</output>
