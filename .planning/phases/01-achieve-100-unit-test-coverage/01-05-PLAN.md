---
phase: 01-achieve-100-unit-test-coverage
plan: 05
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - tests/test_platforms/__init__.py
  - tests/test_platforms/conftest.py
  - tests/test_platforms/test_chatgpt.py
  - tests/test_platforms/test_claude_ai.py
  - tests/test_platforms/test_copilot.py
  - tests/test_platforms/test_deepseek.py
autonomous: true
requirements:
  - COV-CHATGPT
  - COV-CLAUDE-AI
  - COV-COPILOT
  - COV-DEEPSEEK

must_haves:
  truths:
    - "chatgpt.py reaches 100% effective coverage"
    - "claude_ai.py reaches 100% effective coverage"
    - "copilot.py reaches 100% effective coverage"
    - "deepseek.py reaches 100% effective coverage"
  artifacts:
    - path: "tests/test_platforms/test_chatgpt.py"
      provides: "ChatGPT driver tests"
      min_lines: 100
    - path: "tests/test_platforms/test_claude_ai.py"
      provides: "Claude AI driver tests"
      min_lines: 120
    - path: "tests/test_platforms/test_copilot.py"
      provides: "Copilot driver tests"
      min_lines: 100
    - path: "tests/test_platforms/test_deepseek.py"
      provides: "DeepSeek driver tests"
      min_lines: 120
  key_links:
    - from: "tests/test_platforms/"
      to: "skills/orchestrator/engine/platforms/"
      via: "MockPage from conftest.py"
      pattern: "mock_page"
---

<objective>
Cover 4 of 7 platform drivers to 100%: chatgpt.py, claude_ai.py, copilot.py, deepseek.py. Each driver overrides configure_mode, completion_check, extract_response, check_rate_limit plus driver-specific methods.

Purpose: Platform drivers are the largest uncovered surface. Split across two plans to stay within context budget.
Output: tests/test_platforms/ directory with 4 driver test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-achieve-100-unit-test-coverage/01-RESEARCH.md
@.planning/phases/01-achieve-100-unit-test-coverage/01-01-SUMMARY.md
@skills/orchestrator/engine/platforms/chatgpt.py
@skills/orchestrator/engine/platforms/claude_ai.py
@skills/orchestrator/engine/platforms/copilot.py
@skills/orchestrator/engine/platforms/deepseek.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Platform test infrastructure + chatgpt + claude_ai tests</name>
  <files>tests/test_platforms/__init__.py, tests/test_platforms/conftest.py, tests/test_platforms/test_chatgpt.py, tests/test_platforms/test_claude_ai.py</files>
  <read_first>
    - tests/conftest.py (mock_page, install_stubs, _stub_engine_setup)
    - skills/orchestrator/engine/platforms/chatgpt.py (full — ChatGPT class: configure_mode, completion_check, extract_response, check_rate_limit, post_send)
    - skills/orchestrator/engine/platforms/chatgpt_extractor.py (full — used by chatgpt.extract_response)
    - skills/orchestrator/engine/platforms/claude_ai.py (full — ClaudeAI class: configure_mode, completion_check, extract_response, check_rate_limit, inject_prompt)
    - skills/orchestrator/engine/platforms/chrome_selectors.py (selectors used by copilot)
  </read_first>
  <action>
**Create tests/test_platforms/ directory structure:**
- `tests/test_platforms/__init__.py` — empty
- `tests/test_platforms/conftest.py` — shared platform test helpers:
  - Import ENGINE_DIR, _stub_engine_setup from parent conftest
  - Provide a `driver_setup(platform_module, class_name)` fixture/helper that:
    1. Calls install_stubs for the platform
    2. Imports the driver class
    3. Returns an instance with agent_manager=None, prompt_sigs=[]

**tests/test_platforms/test_chatgpt.py:**

Read chatgpt.py to identify every branch. Each method needs tests for:

`check_rate_limit`:
1. `test_rate_limit_usage_cap_found`: locator for usage-cap pattern visible, assert returns pattern string
2. `test_rate_limit_none`: No rate limit elements visible, assert returns None

`configure_mode`:
3. `test_configure_mode_deep_button_found`: "4o" or model selector visible, click succeeds
4. `test_configure_mode_deep_no_button`: No model selector found, assert returns default label
5. `test_configure_mode_regular`: REGULAR mode, assert appropriate model selected

`completion_check`:
6. `test_completion_check_stop_button_visible`: Stop button visible, assert returns False (still generating)
7. `test_completion_check_no_stop_complete`: No stop button, response present, assert returns True
8. `test_completion_check_deep_mode_artifacts`: Check artifact/canvas completion logic

`extract_response`:
9. `test_extract_response_primary_selector`: Main response selector found, returns text
10. `test_extract_response_fallback`: Primary fails, fallback selector works

`post_send`:
11. `test_post_send_deep_research_button`: Deep research start button found, clicked
12. `test_post_send_no_button`: No button found, assert no error

For each: configure mock_page locators to return appropriate count/visibility/text values.

**tests/test_platforms/test_claude_ai.py:**

Read claude_ai.py to identify every branch.

`check_rate_limit`:
1. `test_rate_limit_found`: Usage limit text visible, assert returns pattern
2. `test_rate_limit_none`: No patterns visible

`configure_mode`:
3. `test_configure_mode_deep_extended_found`: Extended thinking toggle found and enabled
4. `test_configure_mode_deep_not_found`: No toggle, fallback
5. `test_configure_mode_regular`: Standard mode

`completion_check`:
6. `test_completion_check_stop_visible`: Stop button visible, returns False
7. `test_completion_check_complete`: No stop, response elements present, returns True

`extract_response`:
8. `test_extract_response_markdown_blocks`: Multiple response blocks, concatenated
9. `test_extract_response_download_artifact`: Download path for artifacts
10. `test_extract_response_short_content`: Very short response

`inject_prompt`:
11. `test_inject_prompt_contenteditable`: Uses execCommand injection
12. `test_inject_prompt_with_attachments`: If claude_ai has attachment handling

Cover all branches including exception handlers (side_effect=Exception on mocks).
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_platforms/test_chatgpt.py tests/test_platforms/test_claude_ai.py -v --tb=short --cov=skills/orchestrator/engine/platforms/chatgpt --cov=skills/orchestrator/engine/platforms/claude_ai --cov-report=term-missing 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - chatgpt.py coverage >= 90%
    - claude_ai.py coverage >= 90%
    - tests/test_platforms/__init__.py exists
    - tests/test_platforms/conftest.py exists
    - `grep -c "def test_" tests/test_platforms/test_chatgpt.py` returns >= 10
    - `grep -c "def test_" tests/test_platforms/test_claude_ai.py` returns >= 10
  </acceptance_criteria>
  <done>chatgpt.py and claude_ai.py at or near 100% coverage</done>
</task>

<task type="auto">
  <name>Task 2: copilot + deepseek driver tests</name>
  <files>tests/test_platforms/test_copilot.py, tests/test_platforms/test_deepseek.py</files>
  <read_first>
    - skills/orchestrator/engine/platforms/copilot.py (full — Copilot class)
    - skills/orchestrator/engine/platforms/deepseek.py (full — DeepSeek class)
    - tests/test_platforms/conftest.py (Task 1 output)
    - skills/orchestrator/engine/platforms/chrome_selectors.py (if copilot uses it)
  </read_first>
  <action>
**tests/test_platforms/test_copilot.py:**

Read copilot.py to identify all branches. Test each method:

`check_rate_limit`: rate limit pattern found vs not found
`configure_mode`: DEEP mode (creative/precise toggle), REGULAR mode, fallback when no toggle
`completion_check`: generating indicator visible (False), no indicator + response present (True)
`extract_response`: primary selector success, fallback paths
`inject_prompt`: copilot-specific injection if overridden

Include exception handling branches (try/except blocks in each method).

**tests/test_platforms/test_deepseek.py:**

Read deepseek.py to identify all branches. DeepSeek has more lines (443) so more branches:

`check_rate_limit`: specific DeepSeek rate limit patterns
`configure_mode`: DeepThink mode toggle, R1 model selection, fallback
`completion_check`: thinking indicator, stop button, response complete
`extract_response`: thinking content extraction, response content, code blocks
`post_send`: DeepSeek-specific post-send behavior if any

For both: use mock_page fixture, configure locator side_effects per test to hit each branch. Test both success and exception paths.

Pattern for each test:
```python
@pytest.mark.asyncio
async def test_copilot_check_rate_limit_found(mock_page):
    install_stubs("copilot", "https://copilot.microsoft.com")
    from platforms.copilot import Copilot
    driver = Copilot()
    # Configure mock_page to return visible rate limit text
    loc = MagicMock()
    loc.first = loc
    loc.count = AsyncMock(return_value=1)
    loc.is_visible = AsyncMock(return_value=True)
    mock_page.get_by_text = MagicMock(return_value=loc)
    result = await driver.check_rate_limit(mock_page)
    assert result is not None
```
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_platforms/test_copilot.py tests/test_platforms/test_deepseek.py -v --tb=short --cov=skills/orchestrator/engine/platforms/copilot --cov=skills/orchestrator/engine/platforms/deepseek --cov-report=term-missing 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - copilot.py coverage >= 90%
    - deepseek.py coverage >= 90%
    - `grep -c "def test_" tests/test_platforms/test_copilot.py` returns >= 10
    - `grep -c "def test_" tests/test_platforms/test_deepseek.py` returns >= 12
  </acceptance_criteria>
  <done>copilot.py and deepseek.py at or near 100% coverage</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → platform drivers | MockPage prevents real browser interaction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | T | platform tests | accept | Mocked browser only |
</threat_model>

<verification>
- chatgpt, claude_ai, copilot, deepseek all >= 90% coverage
- All existing + new tests pass
</verification>

<success_criteria>
4 of 7 platform drivers fully covered with MockPage-based tests.
</success_criteria>

<output>
After completion, create `.planning/phases/01-achieve-100-unit-test-coverage/01-05-SUMMARY.md`
</output>
