---
phase: 01-achieve-100-unit-test-coverage
plan: 07
type: execute
wave: 3
depends_on: ["01-01", "01-02", "01-03", "01-04", "01-05", "01-06"]
files_modified:
  - tests/test_orchestrator.py
  - .github/workflows/ci.yml
  - tests/test_config.py
  - tests/test_collate_responses.py
  - skills/orchestrator/engine/tests/test_chatgpt_rate_limit.py
  - skills/orchestrator/engine/tests/test_deepseek_completion.py
  - skills/orchestrator/engine/tests/test_gemini_completion.py
autonomous: true
requirements:
  - COV-ORCHESTRATOR
  - COV-CONFIG
  - COV-COLLATE
  - COV-CI-GATE
  - COV-INTEGRATION-SKIP

must_haves:
  truths:
    - "orchestrator.py reaches 100% effective coverage"
    - "config.py reaches 100% coverage"
    - "collate_responses.py reaches 100% coverage"
    - "CI yaml has --cov-fail-under=100 and passes"
    - "engine/tests/ files have skip guards"
    - "Full test suite passes with 100% coverage locally"
    - "Zero Missing lines across ALL modules in --cov-report=term-missing before CI gate is committed"
  artifacts:
    - path: "tests/test_orchestrator.py"
      provides: "orchestrator.py async function tests"
      min_lines: 150
    - path: ".github/workflows/ci.yml"
      provides: "CI with --cov-fail-under=100"
      contains: "cov-fail-under=100"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "pytest --cov-fail-under=100"
      via: "CI enforcement"
      pattern: "cov-fail-under"
---

<objective>
Final coverage sweep: test orchestrator.py async functions, fill remaining gaps in config.py and collate_responses.py, add skip guards to engine/tests/, enable --cov-fail-under=100 in CI.

Purpose: This is the finish line -- bring total coverage to 100% and lock it with the CI gate.
Output: Full 100% coverage enforced in CI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-achieve-100-unit-test-coverage/01-RESEARCH.md
@.planning/phases/01-achieve-100-unit-test-coverage/01-01-SUMMARY.md
@skills/orchestrator/engine/orchestrator.py
@skills/orchestrator/engine/config.py
@skills/orchestrator/engine/collate_responses.py
@tests/test_config.py
@tests/test_collate_responses.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Orchestrator async tests + config/collate gap coverage</name>
  <files>tests/test_orchestrator.py, tests/test_config.py, tests/test_collate_responses.py</files>
  <read_first>
    - skills/orchestrator/engine/orchestrator.py (full -- run_single_platform, _staggered_run, _launch_chrome, _gather_with_timeout, _run_all_platforms, orchestrate, main)
    - skills/orchestrator/engine/config.py (identify missing lines from coverage: 12-22, 29-37)
    - skills/orchestrator/engine/collate_responses.py (identify missing lines: 72-73, 79-80, 111-112, 126, 173-181, 185)
    - tests/test_config.py (existing tests -- extend for missing lines)
    - tests/test_collate_responses.py (existing tests -- extend for missing lines)
    - .planning/phases/01-achieve-100-unit-test-coverage/01-RESEARCH.md (Q4: orchestrator testing strategy)
  </read_first>
  <action>
**A. tests/test_orchestrator.py** -- New file:

Stub sys.modules for engine_setup, playwright.async_api, agent_fallback, browser_use, platforms before importing. Use `_stub_engine_setup()` from conftest.

Tests (all `@pytest.mark.asyncio` where async):

`_gather_with_timeout`:
1. `test_gather_with_timeout_returns_results`: Fast tasks complete, returns results list
2. `test_gather_with_timeout_cancels_on_ceiling`: Slow task, short timeout, assert TimeoutError in results
3. `test_gather_with_timeout_mixed`: One fast, one slow, assert fast result + timeout for slow

`run_single_platform`:
4. `test_run_single_platform_success`: Mock context.new_page, mock platform class, assert result dict
5. `test_run_single_platform_uses_condensed`: mode_cfg.use_condensed=True, condensed different from full, assert condensed used
6. `test_run_single_platform_reuses_existing_page`: Pass existing_page, assert context.new_page NOT called
7. `test_run_single_platform_exception`: Platform.run raises, assert status=FAILED result

`_staggered_run`:
8. `test_staggered_run_with_delay`: delay_seconds=2, patch asyncio.sleep, assert sleep called with 2
9. `test_staggered_run_records_usage`: Assert limiter.record_usage called with result status

`_launch_chrome`:
10. `test_launch_chrome_connects_existing`: p.chromium.connect_over_cdp succeeds, assert browser returned
11. `test_launch_chrome_launches_new`: connect fails, subprocess.Popen mocked, assert new launch
12. `test_launch_chrome_fresh_flag`: args.fresh=True, assert no connect attempt

`orchestrate`:
13. `test_orchestrate_all_platforms`: Patch _launch_chrome, _run_all_platforms, assert results returned
14. `test_orchestrate_specific_platforms`: args.platforms="claude_ai,chatgpt", assert only those run
15. `test_orchestrate_unknown_platform_exits`: args.platforms="nonexistent", assert sys.exit(1)
16. `test_orchestrate_skip_rate_check`: args.skip_rate_check=True, assert preflight NOT called

`_run_all_platforms`:
17. `test_run_all_platforms_staggered`: Multiple platforms, assert staggered order used
18. `test_run_all_platforms_exception_to_dict`: One task raises, assert converted to error dict

`main`:
19. `test_main_delegates_to_cli`: Assert `from cli import main as cli_main` is called

**B. tests/test_config.py** -- Extend:

Read the Missing lines (12-22, 29-37). These are likely:
- Platform detection functions (detect_chrome_executable, detect_chrome_user_data_dir)
- OS-specific branches

Add tests:
20. `test_detect_chrome_executable_darwin`: Patch sys.platform="darwin", assert path contains "Google Chrome"
21. `test_detect_chrome_executable_linux`: Patch sys.platform="linux", assert "google-chrome" or "chromium"
22. `test_detect_chrome_user_data_dir_darwin`: Patch sys.platform, assert correct path
23. `test_detect_chrome_user_data_dir_linux`: Patch sys.platform

**C. tests/test_collate_responses.py** -- Extend:

Read the Missing lines (72-73, 79-80, 111-112, 126, 173-181, 185). Add tests to hit:
24. Branches where response files are missing or empty
25. Markdown generation edge cases
26. Summary table formatting branches
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_orchestrator.py tests/test_config.py tests/test_collate_responses.py -v --tb=short --cov=skills/orchestrator/engine/orchestrator --cov=skills/orchestrator/engine/config --cov=skills/orchestrator/engine/collate_responses --cov-report=term-missing 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - orchestrator.py coverage = 100% (0 lines in Missing column)
    - config.py coverage = 100% (0 lines in Missing column)
    - collate_responses.py coverage = 100% (0 lines in Missing column)
    - `grep -c "def test_" tests/test_orchestrator.py` returns >= 15
  </acceptance_criteria>
  <done>orchestrator.py, config.py, collate_responses.py all at 100% coverage (zero Missing lines)</done>
</task>

<task type="auto">
  <name>Task 2: Integration skip guards + pre-gate coverage sweep + CI --cov-fail-under=100</name>
  <files>.github/workflows/ci.yml, skills/orchestrator/engine/tests/test_chatgpt_rate_limit.py, skills/orchestrator/engine/tests/test_deepseek_completion.py, skills/orchestrator/engine/tests/test_gemini_completion.py</files>
  <read_first>
    - .github/workflows/ci.yml (current state)
    - skills/orchestrator/engine/tests/test_chatgpt_rate_limit.py
    - skills/orchestrator/engine/tests/test_deepseek_completion.py
    - skills/orchestrator/engine/tests/test_gemini_completion.py
    - skills/orchestrator/engine/tests/conftest.py
  </read_first>
  <action>
1. **Add skip guards to engine/tests/ integration files:**

For each of these 3 test files, add at the top (after imports):
```python
import os
import pytest

pytestmark = pytest.mark.skipif(
    not os.environ.get("CDP_PORT"),
    reason="Requires live Chrome CDP connection (set CDP_PORT env var)"
)
```

2. **Run full coverage report to identify ALL remaining gaps (pre-gate sweep):**

```bash
python3 -m pytest tests/ -v --tb=short \
  --cov=skills/orchestrator/engine \
  --cov=skills/comparator \
  --cov=skills/landscape-researcher \
  --cov-report=term-missing 2>&1
```

Examine the output. For EVERY module, check the Missing column. The acceptance criterion is: **zero Missing lines across ALL modules**.

3. **Fix any remaining uncovered lines:**
   - For each line in Missing: read the source file at that line number to understand what it does
   - If testable: add a targeted test to the appropriate test file
   - If genuinely untestable (defensive except requiring hardware failure, OS-specific unreachable branch): add `# pragma: no cover` with a comment explaining why
   - Common remaining gaps: except-pass blocks, platform-specific OS branches, edge cases in already-tested modules

4. **Verify zero Missing lines before proceeding to CI gate:**

Re-run the full coverage report. Confirm that EVERY module shows 0 in the Missing column (or only lines excluded by `exclude_lines` config). Do NOT proceed to step 5 until this is confirmed.

5. **Enable --cov-fail-under=100 in CI:**

Update `.github/workflows/ci.yml` "Unit tests with coverage" step to:
```yaml
- name: Unit tests with coverage
  run: |
    python -m pytest tests/ -v --tb=short \
      --cov=skills/orchestrator/engine \
      --cov=skills/comparator \
      --cov=skills/landscape-researcher \
      --cov-report=term-missing \
      --cov-fail-under=100
```

6. **Verify locally that the gate passes:**
```bash
python3 -m pytest tests/ --cov=skills/orchestrator/engine --cov=skills/comparator --cov=skills/landscape-researcher --cov-fail-under=100
```

If it fails, go back to step 3 and fix remaining gaps. Do NOT commit the CI gate until local verification passes.
  </action>
  <verify>
    <automated>python3 -m pytest tests/ -v --tb=short --cov=skills/orchestrator/engine --cov=skills/comparator --cov=skills/landscape-researcher --cov-report=term-missing --cov-fail-under=100 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m pytest tests/ --cov-fail-under=100` exits 0
    - `--cov-report=term-missing` output shows zero Missing lines across ALL modules
    - `grep "cov-fail-under=100" .github/workflows/ci.yml` returns 1 match
    - `grep "pytestmark" skills/orchestrator/engine/tests/test_chatgpt_rate_limit.py` returns 1 match
    - `grep "pytestmark" skills/orchestrator/engine/tests/test_deepseek_completion.py` returns 1 match
    - `grep "pytestmark" skills/orchestrator/engine/tests/test_gemini_completion.py` returns 1 match
    - Total test count > 200 (99 existing + all new tests)
    - No existing tests broken
  </acceptance_criteria>
  <done>100% coverage enforced locally and in CI; zero Missing lines across ALL modules; all integration tests have skip guards</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI -> coverage gate | Prevents future coverage regressions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | T | CI gate | accept | Enforcement mechanism, not attack surface |
</threat_model>

<verification>
- `python3 -m pytest tests/ --cov-fail-under=100` exits 0
- `--cov-report=term-missing` shows zero Missing lines across ALL modules
- All tests pass on Python 3.11+ locally
- CI yaml has the enforcement flag
</verification>

<success_criteria>
Phase complete: 100% unit test coverage enforced in CI with zero Missing lines across all modules, all existing tests pass, all new tests pass, integration tests have skip guards.
</success_criteria>

<output>
After completion, create `.planning/phases/01-achieve-100-unit-test-coverage/01-07-SUMMARY.md`
</output>
